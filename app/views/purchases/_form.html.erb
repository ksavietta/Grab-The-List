<%= simple_form_for [@group, @purchase] do |f| %>
  <% if @purchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@purchase.errors.count, "error") %> prohibited this issue from being saved:</h2>

      <ul>
      <% @purchase.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.input :cost %>
  <%= f.association :items, :as => :check_boxes, :collection => @group.unpurchased_items%>
  <%= f.button :submit %>

  <!-- How do I get the items to be only for a group, not for the user. -->

<% end %>
