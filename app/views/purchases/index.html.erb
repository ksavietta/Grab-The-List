<h1>Purchases</h1>

<table>
  <thead>
    <tr>
      <th>Purchase Name</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @purchases.each do |purchase| %>
      <tr>
        <td><%= link_to purchase.created_at, group_purchase_path(@group,purchase) %></td>
        <td><%= link_to 'Destroy', group_purchase_path(@group,purchase), method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Purchase', new_group_purchase_path(@group) %>