
<%content_for :left_side_bar do %>

  <%= link_to 'Make a Purchase', new_group_purchase_path(@group), class: "button" %>

  <div id="purchases">
    <%= render 'group_purchases' %>
  </div>

<% end %>

<%content_for :main_title do %>
  <div class="medium-12 columns group-name">
    <h1><strong><%=@group.name%></strong></h1>
  </div>

  <%= link_to "Edit Group", edit_group_path(@group)%>

<%end%>


<div class="main-wrapper">

  <ul class= "items-list">
    <% @group.unpurchased_items.each do |item| %>
      <li><i class="fi-target large"></i><span class="item"><%= item.name %></span></li>
    <% end %>
  </ul>

  <div class="items-form">
    <%= render 'items/form', group: @group %>
  </div>

</div>


<%content_for :right_side_bar do%>
  <div id="your-groups">
    <%= render 'members' %>
  </div>

  <br>

<% end %>